services:
  - type: web
    name: bms-backend-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python backend/main.py
    envVars:
      - key: MONGODB_URL
        sync: false
      - key: DATABASE_NAME
        value: bms_production
      - key: SECRET_KEY
        generateValue: true
